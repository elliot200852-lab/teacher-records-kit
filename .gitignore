# ════════════════════════════════════════════════════════════════
# 個資 / 機密 —— 以下一律「絕不」進 git
# ════════════════════════════════════════════════════════════════
# 你的設定（含 email / 專案 id / Drive 夾 id）；範本是 config/*.example.json
config/kit.json
config/tabs.json
# setup.py 重跑時留下的舊設定備份（一樣含 email 與 Firebase 設定）
config/*.bak
# v2 舊設定（老師從 v2 升上來時放在 repo 根目錄，setup.py --upgrade 會把它轉成
# config/kit.json）。v2 的格式長什麼樣看 docs/legacy-config.example.yaml。
config.yaml
# 安裝進度（含你的答案）
setup/progress.json
# 你們學校自己的報告格式（老師貼的，可能含校名與內部欄位）；範本是
# templates/report-format.custom.example.json
config/report-format.custom.json
# 你真實的學生紀錄、名冊、家長通訊錄、台帳、稽核全放這裡
data/
# 錄音檔與逐字稿——永不出本機、永不進 git
inbox/*
!inbox/.gitkeep
# 備份 zip 與匯出快照
backups/
# 一鍵匯出的 Word／PDF／HTML（含名冊真名，永遠不進 git）
exports/
# 離線預覽產生檔
preview/
# 排程與腳本的 log
logs/
# Firebase CLI／模擬器的 debug log
firebase-debug.log
firestore-debug.log
# 排程產生的啟動檔（Windows 工作排程器的 XML；含這台電腦的路徑與帳號）
setup/launchers/
# SMTP App Password 等密鑰
.env
# 服務帳號金鑰
*-key.json
serviceAccount*.json
# 由 build_config.py 產生（含真實 email 與 Firebase 設定）
site/js/kit-config.js
site/js/firebase-config.js
firestore.rules
storage.rules
# 已寄/略過紀錄
.parent-emails-handled.tsv
# 同步狀態
.sync-last-status
# 回寫前的備份
*.prev.md

# ════════════════════════════════════════════════════════════════
# 一般
# ════════════════════════════════════════════════════════════════
.DS_Store
node_modules/
__pycache__/
*.pyc
output/
.firebase/
