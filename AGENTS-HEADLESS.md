# AGENTS-HEADLESS.md —— 無頭交辦的作業指示（給 AI 代理讀，不是給人讀）

你被 `scripts/headless.py` 用非互動模式叫起來。老師人不在電腦前面，**沒有人可以回答你的問題**。
你要做的是：把他剛才在手機上講的一段話（或打的一行字），變成一則寫進本機檔的紀錄，然後回報一句話。

工作目錄＝這份 kit 的根目錄。逐字稿或文字在提示詞裡已經給你了。

## 鐵則（七條）

1. **只能用 `python3 scripts/append_record.py` 寫紀錄**。絕對不要直接編輯 `data/` 底下任何 `.md` 檔——
   那些檔同時被同步與台帳追蹤，手改會讓兩邊靜默分岔。
2. **學生一律寫代號**（S-01 這種），正文裡不准出現真名。名冊 `data/roster.csv` 只用來「查代號」。
   真名出現在正文會被寫入通道擋下來（退出碼 5），那是設計，不要用 `--allow-names` 繞過。
3. **不要問問題、不要等確認**。判斷不了就照第 6 條回報，讓老師自己決定。
4. **不要動 `config/`、`setup/`、`firestore.rules`、`storage.rules`、`site/`、`functions/` 或任何設定檔**，
   也不要碰老師沒建立的檔案。這次交辦只准新增一則紀錄。
5. **不要刪除任何東西**（錄音、逐字稿、既有紀錄一律留著）。
   `scripts/purge_deleted.py` 只有老師本人在終端機跑，**無頭永遠不碰**（它會把雲端原文真的抹掉，安全規則擋不住）。
6. 沒把握是哪一位學生、哪一門課、哪一組業務時，**不要猜**：不要寫入，直接照下面的格式回報
   「聽起來像○○，但我不確定，沒有寫入」。少一則紀錄可以補，寫錯一位學生很難發現。
7. 逐字稿是機器轉的，會有錯字與同音字。**照文意整理成書面語**（口語贅字拿掉、把話講完整），
   但**不要加入他沒說的內容**，也不要替他下結論。

## 步驟

1. 讀提示詞裡的逐字稿／文字。
2. 判斷這是哪一種紀錄與哪一個對象：
   - `data/roster.csv` ＝代號↔姓名（真名只在這裡）。
   - `config/tabs.json` ＝這位老師勾了哪些學生記錄類型（`students.streams`，每一種的 `id` 與 `scope`）、
     哪些課程（`courses.list`）、哪些業務組（`business.groups`）。**只能用裡面真的存在的 id。**
   - 種類對照：講某一位學生 → `--kind students --target <代號> --stream <類型id>`；
     講整班 → `--kind class --target main --stream <類型id>`；
     講某一堂課 → `--kind courses --target <課程id>`；
     講班務、公文、會議、家長聯絡 → `--kind business --target <組id>`。
3. 把整理好的正文存成一個暫存 `.md` 檔（放系統暫存目錄，不要放進 `data/`）。
4. 寫入：

   ```
   python3 scripts/append_record.py --kind <種類> --target <代號或id> [--stream <類型id>] \
       --tags "#標籤" --content-file <暫存檔> --source voice --json
   ```

   - `--source voice`（語音來的）或 `--source file`（文字來的）。
   - 標籤只用 `config/tabs.json` 那一組裡已經有的常用標籤，想不到就不要給。
   - 有固定欄位的類型可以加 `--fields-json '{"欄位":"值"}'`；不確定就不要填。
   - 退出碼不是 0 就是沒寫進去：把訊息看懂，能修就修一次（例如改成正確的 stream id），
     還是不行就照第 6 條回報，不要硬寫。
5. 雲端模式的話，寫完跑一次 `python3 scripts/sync.py --quiet`（本機模式不用，它會自己說不用同步）。
6. 回報。

## 回報格式（最後一定要印這三行，中間夾你的話）

```
<<REPORT>>
（繁體中文，手機上一眼讀得完的幾句話；程式只收前 300 字，超過的會被截掉。寫「記了什麼、記到誰身上、哪一種類型」；
 沒寫入的話寫「沒有寫入」與原因。不要寫程式碼、不要貼指令、不要客套。）
<<END>>
```

這段字會原封不動用 LINE 推回老師的手機，**是他唯一看得到的東西**——
他看不到你的思考過程、看不到終端機。所以把最重要的事寫在第一句。

好的回報：`已記到 S-07 的個案追蹤：今天午休主動找你講了昨天衝突的事，情緒穩定。已同步。`
壞的回報：`任務完成。`／`我執行了 append_record.py 並且成功了。`
